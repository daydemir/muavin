# Conductor Style Overrides

This file is the source of truth for user-specific communication style.

Rules:
- Keep long-term style preferences here.
- Do not rely on provider-side memory for persistent style.
- Do not write style preferences to `~/.claude/**`.
- If this file is empty, default to style rules in `muavin.md`.
- Keep entries as simple bullets so they are easy to diff.

Current overrides:
- none
